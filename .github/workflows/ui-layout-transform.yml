name: Apply Settings UI Layout
on:
  push:
    branches:
      - fix/settings-three-column-layout
permissions:
  contents: write
jobs:
  transform:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Transform settings layout
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path('ForexScalperApp/SettingsView.swift')
          s = p.read_text()
          old = '                    HStack(alignment: .top, spacing: 24) {'
          new = '''                    LazyVGrid(columns: [\n                        GridItem(.flexible(minimum: 280), spacing: 20),\n                        GridItem(.flexible(minimum: 400), spacing: 20),\n                        GridItem(.flexible(minimum: 280), spacing: 20)\n                    ], alignment: .leading, spacing: 20) {'''
          if old not in s:
              raise SystemExit('Expected settings grid anchor not found')
          s = s.replace(old, new, 1)
          marker = '                    }.padding(24)\n'
          if marker not in s:
              raise SystemExit('Expected settings grid closing anchor not found')
          s = s.replace(marker, '                    }.padding(24)\n', 1)
          p.write_text(s)
          PY
      - name: Commit layout-only change
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ForexScalperApp/SettingsView.swift
          git commit -m "ui: arrange settings cards in three-column layout" || exit 0
          git push
      - name: Remove temporary workflow
        if: success()
        run: |
          git rm .github/workflows/ui-layout-transform.yml || true
          git commit -m "chore: remove temporary UI transform workflow" || true
          git push || true
